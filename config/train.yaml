train:
  device: cuda
  batch_size: 64
  learning_rate: 1e-4
  # iterations_per_chunk: 30000
  # validation_freq: 500
  # num_chunks: 1

data_setting:
  val_ratio: 0.1
  test_ratio: 0.1
  inference_size: 3000
  seed: 0